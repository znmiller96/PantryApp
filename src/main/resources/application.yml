spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/pantry
    username: znmiller
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false
application:
  api:
    access: http://localhost:3000/